{
  "name": "punters-den-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "The Punters Den automated tipping pipeline — agents, validators, config.",
  "scripts": {
    "validate": "node validators/validate-slate.js validators/slate_draft.example.json config.json"
  }
}
